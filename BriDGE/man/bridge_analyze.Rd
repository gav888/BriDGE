% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BriDGE.R
\name{bridge_analyze}
\alias{bridge_analyze}
\title{Complete Causal Analysis Pipeline}
\usage{
bridge_analyze(
  data,
  treatment,
  mediators,
  outcome,
  n_bootstraps = 500,
  discovery_method = "mmhc",
  nonlinear = TRUE,
  plot = TRUE,
  parallel = TRUE,
  sensitivity = TRUE,
  handle_convergence = "warn",
  gam_maxit = 200,
  gam_epsilon = 1e-07
)
}
\arguments{
\item{data}{A data frame containing the variables for analysis}

\item{treatment}{Character string specifying the treatment variable name}

\item{mediators}{Character vector specifying the mediator variable names}

\item{outcome}{Character string specifying the outcome variable name}

\item{n_bootstraps}{Integer specifying the number of bootstrap iterations (default: 500)}

\item{discovery_method}{Character string specifying the causal discovery method ("mmhc", "hc", "pc")}

\item{nonlinear}{Logical indicating whether to use nonlinear GAM models (default: TRUE)}

\item{plot}{Logical indicating whether to generate plots (default: TRUE)}

\item{parallel}{Logical indicating whether to use parallel processing (default: TRUE)}

\item{sensitivity}{Logical indicating whether to perform sensitivity analysis (default: TRUE)}

\item{handle_convergence}{Character string specifying how to handle convergence issues ("warn", "simplify", "error")}

\item{gam_maxit}{Integer specifying maximum iterations for GAM fitting (default: 200)}

\item{gam_epsilon}{Numeric specifying convergence tolerance for GAM fitting (default: 1e-7)}
}
\value{
A list containing all analysis results and plots
}
\description{
Main wrapper function that performs a complete causal analysis pipeline including
causal discovery, mediation analysis, sensitivity analysis, and visualization.
}
\examples{
# Generate example data
data <- bridge_generate_data(n = 200) # Smaller n for faster example

# Perform complete analysis
# Reducing bootstraps and disabling parallel for faster example
results <- bridge_analyze(
  data = data,
  treatment = "treatment",
  mediators = c("mediator_1", "mediator_2"),
  outcome = "outcome",
  n_bootstraps = 10,
  parallel = FALSE
)

# View summary
print(results$summary)
}
